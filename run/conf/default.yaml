# Refactored top-level default config.
# The runtime still uses config/loader.py + pydantic schema.
# Keep this file as the canonical root for the new folder layout.

legacy_config: configs/default_config.yaml
